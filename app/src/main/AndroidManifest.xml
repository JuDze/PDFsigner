<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.pdfsigner">

    <!-- Permissions for reading and writing to external storage -->
    <!-- READ_EXTERNAL_STORAGE is implicitly granted with WRITE_EXTERNAL_STORAGE on older APIs -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="35" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="35" />
    <!-- MANAGE_EXTERNAL_STORAGE for Android 11+ to write to public Downloads directory -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <!-- Needed for cryptographic operations to ensure Security.addProvider is allowed -->
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PDFsigner"
        tools:targetApi="35">
        <activity
            android:name="com.example.pdfsigner.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PDFsigner">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.pdfsigner.SignatureActivity"
            android:exported="false"
            android:theme="@style/Theme.PDFsigner"
            android:label="Draw Signature">
        </activity>
    </application>

</manifest>
